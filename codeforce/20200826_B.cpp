#include<iostream>
#include<algorithm>

using namespace std;
int pmax[200004];
int fpax[200004];

int main()
{
	int t;
	scanf("%d", &t);
	for (int i = 0; i < t; i++)
	{
		int p, f, cnts, cntw, s, w;
		scanf("%d %d\n%d %d\n%d %d", &p, &f, &cnts, &cntw, &s, &w);
		for (int j = 0; j < max(cnts, cntw); j++)
		{
			int ps = s * j;
			int ws = 0;
			while(ws<p)

		}
	}

	return 0;
}