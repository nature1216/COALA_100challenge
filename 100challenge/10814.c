#include <stdio.h>

struct person
{
	int age;
	char* name;
};

int main()
{
	int n;
	scanf("%d",n);

	struct person p[100009];
	int i;
	for(int i=0;i<n;i++)
	{
		scanf("%d %s",p[i].age,p[i].name);
	}
	for(i=0;i<)


	return 0;
}